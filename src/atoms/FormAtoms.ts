import { atom } from "recoil";

export const cuentaCardFormState = atom({
    key: 'generalCardFormState',
    default: {},
  });

  export const clienteCardFormState = atom({
    key: 'clienteCardFormState',
    default: {},
  });