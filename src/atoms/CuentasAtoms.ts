import { atom } from "recoil";

export const generalCardFormState = atom({
    key: 'generalCardFormState',
    default: {},
  });

  export const contactoCardFormState = atom({
    key: 'contactoCardFormState',
    default: {},
  });