import { atom } from "recoil";

export const operadorAgregaFormState = atom({
  key: 'operadorAgregaFormState',
  default: {},
});

export const operadorDetalleFormState = atom({
    key: 'operadorDetalleFormState',
    default: {},
});