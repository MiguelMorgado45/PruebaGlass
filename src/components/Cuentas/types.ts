export type GeneralType = {
    cuenta: string,
    fechaAlta : string  | undefined,
    descripcion: string,
}

export type ContactoType = {
    correo: string,
    phone : string
}

export type CuentaFormType = {
    cuenta: string,
    fechaAlta : string  | undefined,
    descripcion: string,
    correo: string,
    phone : string,
}
